syntax = "proto3";

package helium;

message inventory {
  repeated string written_files = 0;
  /// Unix timestamp in seconds of the start of the inventory period
  uint64 start_epoch = 1;
  /// Unix timestamp in seconds of the end of the inventory period
  uint64 end_epoch = 2;
}
