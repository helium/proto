syntax = "proto3";

package helium;

message blockchain_txn_update_network_v1 {
    bytes treasury_key = 1;
    bytes network_signature = 2;

    repeated bytes rollup_keys = 3;
    bytes treasury_signature = 4;

    uint64 fee = 5;
}
